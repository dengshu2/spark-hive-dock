# ============================================================
# Docker Compose - spark-hive-dock (Kerberized + YARN)
#
# Spark SQL cluster with Hive Metastore on Hadoop HDFS,
# secured with MIT Kerberos authentication.
# YARN provides resource management and Kerberos delegation
# token distribution to Spark executors.
#
# Services:
#   - kdc             : MIT Kerberos KDC (authentication center)
#   - mysql           : Metastore backend (MySQL 8.0)
#   - namenode        : HDFS NameNode + YARN ResourceManager
#   - datanode        : HDFS DataNode + YARN NodeManager
#   - hive-metastore  : Hive Metastore Service (metadata only)
#   - spark-connect   : Spark Connect Server (gRPC, YARN client)
#
# Usage:
#   docker compose up -d --build
#   docker compose logs -f spark-connect
#   docker compose down -v   (destroy volumes)
# ============================================================

services:

  # --------------------------------------------------------
  # KDC - MIT Kerberos Key Distribution Center
  # --------------------------------------------------------
  kdc:
    build:
      context: ./kdc
    image: kdc:latest
    container_name: kdc
    hostname: kdc
    domainname: hive-net
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      KRB5_REALM: ${KRB5_REALM:-EXAMPLE.COM}
      KRB5_KDC_PASSWORD: ${KRB5_KDC_PASSWORD:-kdc_admin_2024}
    volumes:
      - keytabs:/etc/security/keytabs
      - krb5kdc-data:/var/lib/krb5kdc
    healthcheck:
      test: ["CMD-SHELL", "test -f /etc/security/keytabs/.kdc-ready"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - hive-net
    restart: unless-stopped

  # --------------------------------------------------------
  # MySQL - Hive Metastore Backend
  # --------------------------------------------------------
  mysql:
    image: mysql:${MYSQL_VERSION:-8.0}
    container_name: mysql
    hostname: mysql
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpass2024}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-hive_metastore}
      MYSQL_USER: ${MYSQL_USER:-hive}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-hive2024}
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpass2024}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - hive-net
    restart: unless-stopped

  # --------------------------------------------------------
  # HDFS NameNode + YARN ResourceManager
  # --------------------------------------------------------
  namenode:
    build:
      context: ./hadoop
      args:
        HADOOP_VERSION: ${HADOOP_VERSION:-3.5.0}
    image: hadoop-base:${HADOOP_VERSION:-3.5.0}
    container_name: namenode
    init: true   # tini PID 1 reaps Hadoop helper-process zombies
    hostname: namenode
    domainname: hive-net
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      HADOOP_ROLE: namenode
    ports:
      - "127.0.0.1:9870:9870"   # HDFS Web UI
      - "127.0.0.1:19000:9000"  # HDFS RPC (remapped to avoid host conflict)
      - "127.0.0.1:8088:8088"   # YARN ResourceManager Web UI
    volumes:
      - namenode-data:/data/namenode
      - keytabs:/etc/security/keytabs:ro
      - ./kdc/krb5.conf:/etc/krb5.conf:ro
    depends_on:
      kdc:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9000 && nc -z localhost 8032"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1536M
    networks:
      - hive-net
    restart: unless-stopped

  # --------------------------------------------------------
  # HDFS DataNode + YARN NodeManager
  # --------------------------------------------------------
  datanode:
    image: hadoop-base:${HADOOP_VERSION:-3.5.0}
    container_name: datanode
    init: true   # tini PID 1 reaps Hadoop helper-process zombies (the ~35 java zombies)
    hostname: datanode
    domainname: hive-net
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      HADOOP_ROLE: datanode
    ports:
      - "127.0.0.1:9864:9864"   # HDFS DataNode Web UI
      - "127.0.0.1:8042:8042"   # YARN NodeManager Web UI
    volumes:
      - datanode-data:/data/datanode
      - keytabs:/etc/security/keytabs:ro
      - ./kdc/krb5.conf:/etc/krb5.conf:ro
    depends_on:
      kdc:
        condition: service_healthy
      namenode:
        condition: service_healthy
    healthcheck:
      # DataNode web UI up AND NodeManager port listening. The port grep is
      # grouped in { } so a curl failure fails the whole check — with a plain
      # `;` the grep alone decided the exit code and the curl was ignored.
      test: ["CMD-SHELL", "curl -sf http://datanode:9864/jmx >/dev/null && PORT_HEX=$$(printf '%04X' 8042) && { grep -q \":$${PORT_HEX} \" /proc/net/tcp6 2>/dev/null || grep -q \":$${PORT_HEX} \" /proc/net/tcp 2>/dev/null; }"]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 5g
    networks:
      - hive-net
    restart: unless-stopped

  # --------------------------------------------------------
  # Hive Metastore (metadata service only)
  # --------------------------------------------------------
  hive-metastore:
    build:
      context: ./hive
      args:
        HIVE_VERSION: ${HIVE_VERSION:-4.1.0}
    image: hive-metastore:${HIVE_VERSION:-4.1.0}
    container_name: hive-metastore
    init: true   # tini PID 1 reaps helper-process zombies
    hostname: hive-metastore
    domainname: hive-net
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpass2024}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-hive_metastore}
      MYSQL_USER: ${MYSQL_USER:-hive}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-hive2024}
    ports:
      - "127.0.0.1:9083:9083"   # Thrift Metastore
    volumes:
      - keytabs:/etc/security/keytabs:ro
      - ./kdc/krb5.conf:/etc/krb5.conf:ro
    depends_on:
      kdc:
        condition: service_healthy
      mysql:
        condition: service_healthy
      namenode:
        condition: service_healthy
      datanode:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "PORT_HEX=$$(printf '%X' 9083); grep -q \":$${PORT_HEX} \" /proc/net/tcp6 2>/dev/null || grep -q \":$${PORT_HEX} \" /proc/net/tcp 2>/dev/null"]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 120s
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - hive-net
    restart: unless-stopped

  # --------------------------------------------------------
  # Spark Connect Server (YARN client mode)
  # --------------------------------------------------------
  spark-connect:
    build:
      context: ./spark
      args:
        SPARK_VERSION: ${SPARK_VERSION:-4.1.2}
        HIVE_VERSION: ${HIVE_VERSION:-4.1.0}
        HIVE_METASTORE_IMAGE: hive-metastore:${HIVE_VERSION:-4.1.0}
    image: spark-hive-custom:${SPARK_VERSION:-4.1.2}
    container_name: spark-connect
    init: true   # tini PID 1 reaps helper-process zombies
    hostname: spark-connect
    domainname: hive-net
    environment:
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "127.0.0.1:15002:15002" # Spark Connect Server (gRPC)
      - "127.0.0.1:4040:4040"   # Spark Application UI
    volumes:
      - keytabs:/etc/security/keytabs:ro
      - ./kdc/krb5.conf:/etc/krb5.conf:ro
    depends_on:
      kdc:
        condition: service_healthy
      namenode:
        condition: service_healthy
      hive-metastore:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "PORT_HEX=$$(printf '%X' 15002); grep -q \":$${PORT_HEX} \" /proc/net/tcp6 2>/dev/null || grep -q \":$${PORT_HEX} \" /proc/net/tcp 2>/dev/null"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 300s
    deploy:
      resources:
        limits:
          memory: 2560M
    networks:
      - hive-net
    restart: unless-stopped

# ============================================================
# Volumes
# ============================================================
volumes:
  mysql-data:
    driver: local
  namenode-data:
    driver: local
  datanode-data:
    driver: local
  keytabs:
    driver: local
  krb5kdc-data:
    driver: local

# ============================================================
# Network
# ============================================================
networks:
  hive-net:
    driver: bridge
    name: hive-net
