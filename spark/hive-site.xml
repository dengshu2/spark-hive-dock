<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  Spark Hive Metastore Client Configuration
  - Connects Spark SQL to the Hive Metastore service
  - Kerberos SASL authentication for Metastore
-->
<configuration>
    <property>
        <name>hive.metastore.uris</name>
        <value>thrift://hive-metastore.hive-net:9083</value>
    </property>
    <property>
        <name>hive.metastore.warehouse.dir</name>
        <value>/user/hive/warehouse</value>
    </property>

    <!-- Kerberos: client-side SASL for Metastore Thrift -->
    <property>
        <name>hive.metastore.sasl.enabled</name>
        <value>true</value>
    </property>
    <property>
        <name>hive.metastore.kerberos.principal</name>
        <value>hive/_HOST@EXAMPLE.COM</value>
    </property>

    <!-- The hive.server2.* block (KERBEROS auth, enable.doAs=false) that used
         to live here served the retired Spark Thrift Server; nothing in the
         Spark Connect / spark-sql paths reads those properties. Removed. -->
</configuration>
